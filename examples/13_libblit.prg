#ifndef OS_WII
import "libblit";
import "mod_say";
#endif

Process main()
Begin
	say(B_ABLEND);
	say(B_ALPHA);
	say(B_HMIRROR);
	say(B_NOCOLORKEY);
	say(B_SBLEND);
	say(B_TRANSLUCENT);
	say(B_VMIRROR);
End;
